run = "npm run dev"
entrypoint = "src/pages/index.astro"
modules = ["nodejs-20", "web"]

[nix]
channel = "stable-25_05"
packages = ["freetype", "gh", "lcms2", "libimagequant", "libjpeg", "libtiff", "libwebp", "libxcrypt", "openjpeg", "tcl", "tk", "unzip", "zip", "zlib", "ffmpeg"]

[env]
PATH = "/home/runner/${REPL_SLUG}/node_modules/.bin:${PATH}"

[packager]
language = "nodejs"

[packager.features]
packageSearch = true
guessImports = true
enabledForHosting = false

[languages]

[languages.javascript]
pattern = "**/{*.js,*.jsx,*.ts,*.tsx,*.astro}"

[languages.javascript.languageServer]
start = "typescript-language-server --stdio"

[[ports]]
localPort = 4321
externalPort = 3000

[[ports]]
localPort = 5000
externalPort = 80

[deployment]
run = ["node", "server.mjs"]
deploymentTarget = "static"
build = ["npm", "run", "build"]
publicDir = "dist"

[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[workflows.workflow.metadata]
outputType = "webview"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd /home/runner/workspace && npm run build && npm run preview -- --port 5000"
waitForPort = 5000
